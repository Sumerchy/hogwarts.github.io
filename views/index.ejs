<!doctype html>
<html>
	<head>
	</head>
	<body>
		<form action="/gradebook" method="POST">
			<input type="text" name="student" placeholder="Enter students first name">
			<input type="submit" value="push">
		</form>
	</body>
</html>

Этот файл выводит форму, данные с неё отправляются на роут /gradebook. Шаблон страницы gradebook.ejs приведён ниже.
<!doctype html>
<html>
	<head>
		<meta charset="utf-8">
	</head>
	<body>
		<% if(rows.length > 0) {%>
			<%=rows[0].фамилия%>
			<%} else{%>
				Нет оценок
				<%}%>
		<ul>
			<% for(let i=0; i < rows.length; i++){%>
			<li><%= rows[i].название %> <%= rows[i].получил_оценку%></li>
			<%}%>
		</ul>
		<a href="/">Главная страница</a>
	</body>
</html>
